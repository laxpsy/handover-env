[project]
name = "handover-env"
version = "0.1.0"
description = "Raylib based cellular handover gymnasium compatible env"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.3",
    "raylib>=5.5.0.4",
]
[tool.black]
line-length = 79
